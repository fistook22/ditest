import random

with open("./word_list") as f:
    def get_words_from_file(self):
        f.read([])

    def get_random_sentence(self, length):
        self.length = length
        f.random(length)

    def create_sentence():
        return f.lower()

    def main():
        print("this program has a few functions using words from a specific file, at events it randomly_generates sentences, collections etc..")

    def user_in(self):
        user_input = input("how long you want your sentence to be? enter a number between 2-20.")
            try:
                user_input = self.length
            except:
                return ValueError
	